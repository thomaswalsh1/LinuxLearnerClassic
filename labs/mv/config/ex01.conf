id=m-f
title=mv -f (force)
description=Test the -f (force) option for mv, which overwrites destination files without prompting.
lab_dir=labs/mv/ex01
validator=validate_mv_f
target_file=output.txt
hint=Use mv -f to overwrite dest/file1 with source/file1.
completed=0
preserve_sandbox=1
