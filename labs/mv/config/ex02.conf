id=m-i
title=mv -i (interactive)
description=Test the -i (interactive) option for mv, which prompts before overwriting destination files.
lab_dir=labs/mv/ex02
validator=validate_mv_i
target_file=dest/file1
hint=Use mv -i and respond to the prompt to control overwriting dest/file1.
completed=0
preserve_sandbox=1
