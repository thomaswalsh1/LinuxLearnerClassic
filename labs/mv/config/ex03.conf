id=m-n
title=mv -n (no-clobber)
description=Test the -n (no-clobber) option for mv, which prevents overwriting destination files.
lab_dir=labs/mv/ex03
validator=validate_mv_n
target_file=dest/file1
hint=Use mv -n to avoid overwriting dest/file1 with source/file1.
completed=0
preserve_sandbox=1
