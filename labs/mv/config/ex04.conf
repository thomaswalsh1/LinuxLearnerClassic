id=m-u
title=mv -u (update)
description=Test the -u (update) option for mv, which only overwrites if the source file is newer than the destination.
lab_dir=labs/mv/ex04
validator=validate_mv_u
target_file=dest/file1
hint=Use mv -u to overwrite dest/file1 only if source/file1 is newer.
completed=0
preserve_sandbox=1
