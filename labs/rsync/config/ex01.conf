id=rsync-a
title=rsync -a (archive)
description=Test the -a (archive) option for rsync, which enables archive mode for copying files and directories.
lab_dir=labs/rsync/ex01
validator=validate_rsync_a
target_file=dest/
hint=Use rsync -a to copy all files and directories from source/ to dest/.
completed=0
preserve_sandbox=1
