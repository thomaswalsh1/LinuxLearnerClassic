id=rsync-g
title=rsync -g (group)
description=Test the -g option for rsync, which retains the file's group.
lab_dir=labs/rsync/ex03
validator=validate_rsync_g
target_file=dest/
hint=Use rsync -g to copy files from source/ to dest/ and retain group ownership.
completed=0
preserve_sandbox=1
