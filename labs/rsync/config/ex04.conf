id=rsync-h
title=rsync -h (human-readable)
description=Test the -h option for rsync, which displays output in a human-readable format.
lab_dir=labs/rsync/ex04
validator=validate_rsync_h
target_file=dest/
hint=Use rsync -h to copy files and observe human-readable output.
completed=0
preserve_sandbox=1
