id=rsync-p
title=rsync -p (perms)
description=Test the -p option for rsync, which retains file permissions.
lab_dir=labs/rsync/ex07
validator=validate_rsync_p
target_file=dest/
hint=Use rsync -p to copy files from source/ to dest/ and retain permissions.
completed=0
preserve_sandbox=1
