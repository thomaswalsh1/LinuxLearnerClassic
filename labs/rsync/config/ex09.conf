id=rsync-r
title=rsync -r (recursive)
description=Test the -r option for rsync, which copies directories recursively.
lab_dir=labs/rsync/ex09
validator=validate_rsync_r
target_file=dest/
hint=Use rsync -r to copy all files and subdirectories from source/ to dest/.
completed=0
preserve_sandbox=1
