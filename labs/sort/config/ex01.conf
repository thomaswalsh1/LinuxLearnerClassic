id=sort-01
title=Exercise 1: Check if Sorted
description=Verify if data.txt is already sorted. If not, sort it and verify.
lab_dir=labs/sort/ex01
validator=sort_check
target_file=data.txt
hint=sort data.txt && sort -c data.txt
completed=0
